<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frelance0">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frelance0">
        <activity android:name=".loading"></activity>
        <activity android:name=".Users" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Edit_Profile" />
        <activity
            android:name=".Client_Profile"
            android:label="@string/title_activity_client__profile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".C_Sign_in" />
        <activity android:name=".C_Sign_up" />
        <activity android:name=".Freelancer_Log_in" />
        <activity android:name=".Freelancer_Profile" />
        <activity android:name=".F_Sign_Up" />
        <activity android:name=".Choose" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>